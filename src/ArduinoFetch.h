#include "utils/FormData.h"
#include "utils/URLEncoded.h"
#include "utils/JSON.h"

class ArduinoFetch {
    ArduinoFetch();
};

class Headers {
    Headers();
};

class Body {
    Body();
};

class Request {
    Request();
};

class Response {
    Response();
};

class RequestOptions {
    RequestOptions();
};